using System;
using Microsoft.AspNetCore.Identity;

namespace FactorioProductionCells.Infrastructure.Identity
{
    public class NetCoreUser : IdentityUser<Guid>
    {
        //TODO: Do I really not have to add anything else here?????

        /*
        AccessFailedCount
        Claims
        ConcurrencyStamp
        Email
        EmailConfirmed
        Id
        LockoutEnabled
        LockoutEnd
        Logins
        NormalizedEmail
        NormalizedUserName
        PasswordHash
        PhoneNumber
        PhoneNumberConfirmed
        Roles
        SecurityStamp
        TwoFactorEnabled
        UserName
        */
    }
}
